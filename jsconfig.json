{
    "compilerOptions": {
      "baseUrl": "src",
      "paths": {
        "$assets": ["assets"],
        "$comp": ["comp"],
        "$editor": ["editor"],
        "$utils":["utils"]
      }
    },
    "include": ["src/**/*.ts"],
    "exclude": ["node_modules", "**/node_modules", "dist"]
  }