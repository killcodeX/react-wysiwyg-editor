{
    "compilerOptions": {
      "baseUrl": "src",
      "paths": {
        "$assets": ["assets"],
        "$comp": ["comp"],
        "$editor": ["editor"],
      }
    },
    "include": ["src/**/*.ts"],
    "exclude": ["node_modules", "**/node_modules", "dist"]
  }